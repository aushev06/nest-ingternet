export enum StatusEnum {
  ACTIVE = 'active',
  BLOCKED = 'blocked',
  PENDING = 'pending',
  REJECTED = 'rejected',
  STOPPED = 'stopped',
  DISABLED = 'disabled',
}

// active,Active
// blocked,Blocked
// pending,Pending
// rejected,Rejected
// stopped,Stopped
